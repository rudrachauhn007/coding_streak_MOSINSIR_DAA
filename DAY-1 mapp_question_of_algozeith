// shree ganeshaya namaha:|
// shree chamudamtaya namaha:|
// shree saraswatimataya namaha:|
#include <bits/stdc++.h>
using namespace std;

#define endl '\n' 

void solve ()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        vector<int> v(n);
        map<int,int> mpp;
        for (int i=0;i<n;i++)
        {
            cin >> v[i];
            mpp[v[i]]++;
        }
        cout << mpp.size() << endl;
    }
    
}

signed main() {
    ios::sync_with_stdio(false);  
    cin.tie(NULL);               
    cout.tie(NULL);              

    int t=1;
    while (t--)
    {
        solve();
    }

    return 0;
}


link to question = https://maang.in/problems/Concatenate-Array-AZ101-373?resourceUrl=cs137-cp597-pl4022-rs373&returnUrl=%5B%22%2Fcourses%2FIndian-CP-Summer-CampStructured-137%3Ftab%3Dchapters%22%5D
